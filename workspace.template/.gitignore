# Runtime; never part of the workspace's behaviour history.
run/

# Delivered context feeds (data, not behaviour — see docs/context-feeds.md)
context/

# Generated by pepperd at startup (per-deployment paths)
tools/pepperctl

# Inbound file uploads (runtime data, not behaviour history)
uploads/
